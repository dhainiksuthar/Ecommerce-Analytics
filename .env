# --- General Project Settings ---
AIRFLOW_PROJ_DIR=.
ENV_FILE_PATH=./.env

# --- Airflow Image Version ---
AIRFLOW_IMAGE_NAME=apache/airflow:2.10.0

# --- Airflow Identity (Crucial for Windows/WSL2) ---
# On Windows, 50000 is the standard default for the Airflow user
AIRFLOW_UID=50000

# --- Python Dependencies ---
# Add any libraries you need for your ETL (e.g., pandas, kafka-python)
# Note: Dependencies are now baked into the Docker image via requirements.txt
# _PIP_ADDITIONAL_REQUIREMENTS=pandas kafka-python pyspark